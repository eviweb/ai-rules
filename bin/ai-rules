#!/usr/bin/env python3
"""Development entry point — run without installing the package."""
import sys
import os

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

from ai_rules.cli import app

if __name__ == "__main__":
    app()
